@{
    ViewData["Title"] = "Authentication Page";
}
<div class="row justify-content-center mt-5">

    <h1 class="col-11 ">Jump start your project today!</h1>
</div>
<div class="row justify-content-around ">
    <div class="col-5 mt-5">@await Html.PartialAsync("Partials/_Register")</div>
    <div class="col-5 mt-5">@await Html.PartialAsync("Partials/_Login")</div>
</div>